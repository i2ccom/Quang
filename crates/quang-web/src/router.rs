//! Shared route type — re-exported from the app crate.
//!
//! The full route definition is in `quang-hub-app` where all
//! crate dependencies converge. This module exists as a placeholder
//! for shared route utilities.

// Route type placeholder — actual routes defined in quang-hub-app
pub type AppRoute = ();
