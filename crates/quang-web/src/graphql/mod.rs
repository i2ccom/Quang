//! GraphQL schema & query definitions for QuangHub.
//!
//! Defines the GraphQL schema types and query operations used across
//! all modules. In MVP, these are hand-typed; in future, they
//! auto-generate from `@model` declarations.

pub mod schema;
